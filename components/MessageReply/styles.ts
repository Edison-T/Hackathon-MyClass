import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        padding: 5,
    },
    messageBox: {
        borderRadius: 10,
        padding: 10,
    },
    name: {
        color: "#00BFFF",
        fontWeight: 'bold',
        marginLeft: 7.5,
        fontSize: 13,
    },
    message: {
        fontSize: 15,
        marginTop: 10,
    },
});

export default styles;